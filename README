UCI compatible chess engine! Just build and then load the executable in any valid chess program. The AI that is included uses a negamax algorithm with alpha-beta pruning, and
its depth can be customized. Will go around 20 moves with Stockfish
